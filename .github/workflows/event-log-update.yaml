name: "Fire event after log file update"
on:
        push:
                branches:
                        - main
                paths:
                        - "file.log"
jobs:
        fire-event:
                runs-on: ubuntu-latest
                steps:
                        - uses: actions/checkout@v2
                        - name: fire-event
                          run: echo "file.log has been updated recently !"

